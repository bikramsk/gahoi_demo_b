'use strict';

/**
 * supported-student service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::supported-student.supported-student');
